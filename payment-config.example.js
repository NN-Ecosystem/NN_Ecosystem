// Copy to payment-config.js during deployment. This is a PUBLIC Paddle client-side token, never an API key.
window.NN_PAYMENT = {
  paddleEnvironment: "sandbox",
  paddleClientToken: "test_REPLACE_WITH_PADDLE_CLIENT_SIDE_TOKEN"
};
