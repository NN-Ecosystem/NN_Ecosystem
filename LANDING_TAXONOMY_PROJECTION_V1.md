# Landing Taxonomy Projection v1

Landing now starts product discovery with Domain -> Family/Application while the
existing Engine/Plugin/Pipeline/Node sections remain available as technical
detail projections. The page never invents taxonomy: when signed catalog rows
lack `domain_id` or `family_ids`, it explicitly requests a Product Catalog sync.
